import 'package:capar/core/utils/enums.dart';
import 'package:flutter/material.dart';

class CommunalScreen extends StatelessWidget {
  enmPaymentType type;
  CommunalScreen(this.type);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
