import 'package:flutter/cupertino.dart';

class HistoryTicket extends StatelessWidget {
  const HistoryTicket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("History ticket");
  }
}
